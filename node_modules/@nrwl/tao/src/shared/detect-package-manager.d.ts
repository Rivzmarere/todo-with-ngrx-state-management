/// <reference types="node" />
import { virtualFs } from '@angular-devkit/core';
import { Stats } from 'fs';
export declare function detectPackageManager(host: virtualFs.Host<Stats>): Promise<string>;
